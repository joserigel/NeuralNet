#include "Loss.hpp"

Expression* MeanSquaredError::calculate(
    Layer* model,
    std::vector<std::vector<float>> inputs, 
    std::vector<std::vector<float>> labels
    ) {
        for (unsigned int i = 0; i < inputs.size(); i++) {
            
        }
        return 0;
}